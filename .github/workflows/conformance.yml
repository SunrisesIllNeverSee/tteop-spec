name: Conformance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  conformance:
    name: TTEOP conformance suite
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0  # DCO check needs commit history

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Gate 1: Install dependencies reproducibly
      - name: 1. Install dependencies (npm ci)
        run: npm ci

      # Gate 2: Run the TTEOP conformance runner
      - name: 2. Run TTEOP conformance runner
        run: node conformance/tteop-runner.mjs

      # Gate 3: Run runner self-tests proving failures are detected
      - name: 3. Run runner self-test
        run: python3 conformance/tests/runner-self-test.py

      # Gate 4: Validate every canonical valid envelope
      - name: 4. Validate all canonical examples
        run: |
          for f in examples/minimal-valid.json examples/complete-valid.json examples/unsupported-cache.json examples/signed-envelope.json examples/canonical-reference.json; do
            echo "Validating $f..."
            node conformance/tteop-validate.mjs "$f" --report text
          done

      # Gate 5: Confirm every invalid/adversarial fixture is rejected
      - name: 5. Run negative-fixture suite
        run: node conformance/tests/negative-fixtures.mjs

      # Gate 6: Install the Python package
      - name: 6. Install Python package
        run: pip install -e python/ --quiet

      # Gate 7: Validate output from the installed Python package's public builder
      - name: 7. Validate Python package build_envelope output
        run: |
          python3 -c "from sigrank_standard import build_record; import json; r = build_record(1251211, 11296121, 128196310, 2555179769, provider='anthropic', model='claude-sonnet-4', tool='claude-code'); print(json.dumps(r, indent=2))" > /tmp/pkg-record.json
          node conformance/tteop-validate.mjs /tmp/pkg-record.json --report text

      # Gate 7b: Run Python package tests
      - name: 7b. Run Python package tests
        run: |
          pip install pytest --quiet
          python3 -m pytest python/tests/test_build_envelope.py -v

      # Gate 8: Validate TypeScript integration output
      - name: 8. Validate TypeScript integration output
        run: |
          node -e "
            import { buildEnvelope } from './integrations/typescript/example.ts';
            const record = buildEnvelope(
              { input: 1251211, output: 11296121, cache_write: 128196310, cache_read: 2555179769 },
              { provider: 'anthropic', model: 'claude-sonnet-4', tool: 'claude-code' }
            );
            const fs = await import('fs');
            fs.writeFileSync('/tmp/ts-record.json', JSON.stringify(record, null, 2));
            console.log('TS record written');
          " --input-type=module
          node conformance/tteop-validate.mjs /tmp/ts-record.json --report text

      # Gate 9: Validate CLI integration output
      - name: 9. Validate CLI integration output
        run: |
          node integrations/cli/example.mjs --input 1251211 --output 11296121 --cache-write 128196310 --cache-read 2555179769 > /tmp/cli-record.json
          node conformance/tteop-validate.mjs /tmp/cli-record.json --report text

      # Gate 10: Validate MCP integration via real tools/call JSON-RPC exchange
      - name: 10. Validate MCP integration output (tools/call JSON-RPC)
        run: |
          set -e
          # Send a real tools/call JSON-RPC request to the MCP server
          echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_tteop_record","arguments":{"input":1251211,"output":11296121,"cache_write":128196310,"cache_read":2555179769,"tool":"claude-code","provider":"anthropic","model":"claude-sonnet-4"}}}' | node integrations/mcp/example.mjs > /tmp/mcp-response.json
          # Verify the response is non-empty and valid JSON-RPC
          test -s /tmp/mcp-response.json || { echo "FAIL: MCP response is empty"; exit 1; }
          # Extract the envelope from the JSON-RPC response
          python3 -c "
          import json, sys
          with open('/tmp/mcp-response.json') as f:
              resp = json.load(f)
          assert resp.get('jsonrpc') == '2.0', f'Not JSON-RPC 2.0: {resp}'
          assert resp.get('id') == 1, f'id mismatch: {resp.get(\"id\")}'
          assert 'error' not in resp, f'MCP returned error: {resp[\"error\"]}'
          assert 'result' in resp, 'MCP response missing result'
          env = json.loads(resp['result']['content'][0]['text'])
          with open('/tmp/mcp-record.json', 'w') as f:
              json.dump(env, f, indent=2)
          print('MCP envelope extracted and non-empty')
          "
          test -s /tmp/mcp-record.json || { echo "FAIL: extracted MCP record is empty"; exit 1; }
          node conformance/tteop-validate.mjs /tmp/mcp-record.json --report text

      # Gate 10b: Validate MCP tools/list
      # Asserts the published inputSchema advertises the same runtime maximum
      # (Number.MAX_SAFE_INTEGER) that validateTokenField enforces, on all four
      # token properties, so MCP clients cannot generate oversized values that
      # the server would subsequently reject (CodeRabbit minor #1).
      - name: 10b. Validate MCP tools/list
        run: |
          echo '{"jsonrpc":"2.0","id":2,"method":"tools/list"}' | node integrations/mcp/example.mjs > /tmp/mcp-tools.json
          python3 -c "
          import json
          f=open('/tmp/mcp-tools.json'); resp=json.load(f); f.close()
          assert resp['jsonrpc']=='2.0','Not JSON-RPC'
          assert len(resp['result']['tools'])==1,'Expected 1 tool'
          tool=resp['result']['tools'][0]
          assert tool['name']=='get_tteop_record','Wrong tool name'
          props=tool['inputSchema']['properties']
          MAX=9007199254740991
          for field in ('input','output','cache_write','cache_read'):
              assert field in props, f'missing token field {field}'
              assert props[field].get('minimum')==0, f'{field} missing minimum:0'
              assert props[field].get('maximum')==MAX, f'{field} missing maximum:{MAX} (got {props[field].get(\"maximum\")})'
          print('MCP tools/list verified (4 token fields advertise minimum:0 + maximum:MAX_SAFE_INTEGER)')
          "

      # Gate 11: Exercise half-even tie vectors across implementations
      - name: 11. Run cross-implementation tie tests
        run: node conformance/tests/tie-tests.mjs

      # Gate 12: Generate and schema-validate JSON conformance reports
      - name: 12. Validate conformance reports against schema
        run: node conformance/tests/report-schema-test.mjs

      # Gate 13: Builder regression — missingness semantics on every public surface
      - name: 13. Builder regression tests (TS, CLI, MCP missingness)
        run: node conformance/tests/builder-regression-test.mjs

      # Gate 13b: Legacy alias acceptance (C6 — otep/0.1-draft + sigrank/0.1-draft)
      - name: 13b. Legacy alias acceptance tests
        run: node conformance/tests/alias-acceptance-test.mjs

      # Gate 13c: Stable builder API (lib/envelope-builder.mjs) — buildEnvelope
      # produces schema+semantic-valid envelopes across the documented input
      # matrix; all documented error conditions throw.
      - name: 13c. Stable builder API tests
        run: node conformance/tests/builder-api-test.mjs

      # Gate 14: Exercise the installed 'tteop' executable
      - name: 14. Exercise installed tteop executable
        run: |
          npm link --quiet
          tteop version
          tteop help | head -3
          tteop validate examples/complete-valid.json --report text
          tteop validate examples/complete-valid.json --report json > /tmp/tteop-cli-report.json
          tteop validate examples/complete-valid.json --class producer --report text
          tteop validate examples/complete-valid.json --class consumer --report text
          tteop validate examples/negative/08-public-pseudo-cohort.json --report json || echo "Correctly rejected negative fixture"

      # Gate 15: Check for DCO sign-off on new commits (event-aware)
      - name: 15. Check DCO sign-off
        run: bash scripts/check-dco.sh
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_BASE_REF: ${{ github.base_ref }}
          GITHUB_PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GITHUB_BEFORE: ${{ github.event.before }}
          GITHUB_AFTER: ${{ github.event.after }}

      # Gate 16: git diff --check
      - name: 16. git diff --check
        run: git diff --check
